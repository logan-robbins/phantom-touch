<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Allow cleartext (ws://) for local development and relay connections.
         In production, use wss:// via Tailscale or Caddy TLS termination. -->
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
